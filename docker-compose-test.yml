services:

  wiremock:
    image: wiremock/wiremock
    networks:
    - hmpps_int
    container_name: wiremock
    restart: always
    volumes:
      - ./integration_tests/wiremock:/home/wiremock
    ports:
      - "9091:8080"

networks:
  hmpps_int:
